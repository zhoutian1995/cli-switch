---
phase: 01-runtime-contract-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/resolver/service.ts
  - cmd/doctor.ts
  - cmd/run.ts
  - test/unit/resolver-contract.test.ts
  - test/e2e/cli-json.test.ts
autonomous: true
requirements: [RT-02, RT-03]
user_setup: []
must_haves:
  truths:
    - Platform and binary preflight failures are reported before spawning an agent when runtime target is known.
    - Resolver remains the source of truth for supported platform and required binary checks.
  artifacts:
    - Tests cover PLATFORM_UNSUPPORTED and BINARY_NOT_FOUND in resolver and at least one user-facing command path.
  key_links:
    - run/doctor behavior reuses resolver-compatible checks instead of inventing divergent logic.
---

<objective>
Unify platform and binary preflight across resolve, doctor, and run where applicable.

Purpose: Prevent avoidable agent spawn failures and make runtime diagnostics consistent.
Output: Shared or consistently reused preflight checks with command-level tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-contract-closure/01-CONTEXT.md
@docs/specs/runtime-spec.md
@src/core/resolver/service.ts
@cmd/doctor.ts
@cmd/run.ts
@test/unit/resolver-contract.test.ts
@test/e2e/cli-json.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locate preflight divergence</name>
  <files>cmd/doctor.ts, cmd/run.ts, src/core/resolver/service.ts</files>
  <read_first>cmd/resolve.ts, cmd/doctor.ts, cmd/run.ts, src/core/resolver/service.ts</read_first>
  <action>Compare how resolve, doctor, and run determine the target tool/agent and whether platform/binary constraints are checked. Identify the smallest reuse point that avoids spawning an agent when a required binary is missing or platform is unsupported.</action>
  <verify>npm test -- test/unit/resolver-contract.test.ts</verify>
  <acceptance_criteria>
    - The implementation has one clear source of truth for platform/binary checks.
    - Existing resolver tests still pass.
  </acceptance_criteria>
  <done>Preflight ownership is clear and not duplicated inconsistently.</done>
</task>

<task type="auto">
  <name>Task 2: Wire preflight into user-facing paths</name>
  <files>cmd/doctor.ts, cmd/run.ts, test/e2e/cli-json.test.ts</files>
  <read_first>cmd/_shared.ts, cmd/doctor.ts, cmd/run.ts, test/e2e/cli-json.test.ts</read_first>
  <action>Add preflight reporting to doctor and run only where the target agent/tool is known. For JSON output, use existing envelope helpers. For text output, print concise actionable errors. Do not block dry-run unless the selected target is invalid and the path already has enough information to prove it.</action>
  <verify>npm test -- test/e2e/cli-json.test.ts test/e2e/run-command.test.ts</verify>
  <acceptance_criteria>
    - JSON failures use documented error codes.
    - Text failures are concise and actionable.
    - Existing dry-run happy paths still pass.
  </acceptance_criteria>
  <done>User-facing commands surface preflight issues consistently.</done>
</task>

<task type="auto">
  <name>Task 3: Add preflight tests</name>
  <files>test/unit/resolver-contract.test.ts, test/e2e/cli-json.test.ts</files>
  <read_first>test/unit/resolver-contract.test.ts, test/e2e/cli-json.test.ts</read_first>
  <action>Add focused tests for PLATFORM_UNSUPPORTED and BINARY_NOT_FOUND on the chosen user-facing command path. Prefer fixtures or local registry mutation over depending on the developer machine missing real binaries.</action>
  <verify>npm test -- test/unit/resolver-contract.test.ts test/e2e/cli-json.test.ts</verify>
  <acceptance_criteria>
    - Tests are deterministic on machines with or without Claude/Codex installed.
    - Error code assertions use exact strings.
  </acceptance_criteria>
  <done>Preflight behavior is regression-protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test -- test/unit/resolver-contract.test.ts test/e2e/cli-json.test.ts test/e2e/run-command.test.ts
- [ ] npm run build
- [ ] git diff --check
</verification>

<success_criteria>

- RT-02 and RT-03 are satisfied.
- No agent process is spawned when a known preflight failure should stop execution.
- Summary created at `.planning/phases/01-runtime-contract-closure/01-02-SUMMARY.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-contract-closure/01-02-SUMMARY.md`
</output>
