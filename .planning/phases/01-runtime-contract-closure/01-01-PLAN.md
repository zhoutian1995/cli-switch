---
phase: 01-runtime-contract-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/resolver/service.ts
  - src/core/resolver/utils.ts
  - test/unit/resolver-contract.test.ts
autonomous: true
requirements: [RT-01]
user_setup: []
must_haves:
  truths:
    - Explicit incompatible provider/vendor/transport combinations fail with RESOLVE_CONFLICT.
    - Existing compatible resolve requests still succeed.
  artifacts:
    - Resolver contract tests cover provider missing, provider/tool mismatch, vendor mismatch, and transport mismatch.
  key_links:
    - cmd/resolve JSON errors continue to surface resolver diagnostics through the existing envelope.
---

<objective>
Audit and tighten provider/vendor/transport resolver contracts.

Purpose: Make explicit runtime selection failures deterministic and test-covered before expanding config and sandbox behavior.
Output: Updated resolver validation and contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-contract-closure/01-CONTEXT.md
@docs/PRD.md
@docs/IMPLEMENTATION_GAP_REPORT.md
@docs/specs/routing-spec.md
@src/core/resolver/service.ts
@src/core/resolver/model-resolver.ts
@src/registry/builtins/providers.toml
@src/registry/builtins/models.toml
@test/unit/resolver-contract.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit provider resolution invariants</name>
  <files>src/core/resolver/service.ts, test/unit/resolver-contract.test.ts</files>
  <read_first>src/core/resolver/service.ts, src/core/resolver/model-resolver.ts, src/registry/builtins/providers.toml, src/registry/builtins/models.toml</read_first>
  <action>Identify all paths where request.provider, request.vendor, and request.transport can override or conflict with model/profile defaults. Keep compatible fallback behavior, but make explicit incompatible requests fail with RESOLVE_CONFLICT and structured details. Do not introduce config loading in this plan.</action>
  <verify>npm test -- test/unit/resolver-contract.test.ts</verify>
  <acceptance_criteria>
    - Resolver still returns ok=true for a default claude-code resolve.
    - Explicit incompatible provider/vendor/transport cases return diagnostics[0].code === "RESOLVE_CONFLICT".
  </acceptance_criteria>
  <done>Provider resolution behavior is explicit, deterministic, and covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Expand conflict details</name>
  <files>src/core/resolver/service.ts, src/core/resolver/utils.ts</files>
  <read_first>src/core/resolver/service.ts, src/core/resolver/utils.ts</read_first>
  <action>Ensure RESOLVE_CONFLICT diagnostics include enough details for callers to explain what conflicted: requested provider/vendor/transport, resolved model vendor/transport, provider supported tools, and provider transports when relevant. Preserve current error envelope shape.</action>
  <verify>npm test -- test/unit/resolver-contract.test.ts test/e2e/cli-json.test.ts</verify>
  <acceptance_criteria>
    - Conflict diagnostics include machine-readable details.
    - Existing CLI JSON golden tests still pass.
  </acceptance_criteria>
  <done>Conflict errors are actionable without changing the public envelope.</done>
</task>

<task type="auto">
  <name>Task 3: Document runtime contract closure status</name>
  <files>docs/IMPLEMENTATION_GAP_REPORT.md, docs/specs/routing-spec.md</files>
  <read_first>docs/IMPLEMENTATION_GAP_REPORT.md, docs/specs/routing-spec.md</read_first>
  <action>Update the relevant P0 status notes to distinguish completed strict resolver behavior from remaining preflight/error-code work. Do not mark Phase 1 complete unless all three Phase 1 plans are done.</action>
  <verify>git diff --check -- docs/IMPLEMENTATION_GAP_REPORT.md docs/specs/routing-spec.md</verify>
  <acceptance_criteria>
    - Docs accurately describe resolver strictness status after this plan.
    - Docs still identify platform/binary and error-code closure as separate remaining work.
  </acceptance_criteria>
  <done>Documentation reflects the resolver contract state precisely.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test -- test/unit/resolver-contract.test.ts test/e2e/cli-json.test.ts
- [ ] npm run build
- [ ] git diff --check
</verification>

<success_criteria>

- RT-01 is satisfied.
- No compatible default resolve behavior regresses.
- Conflict diagnostics are structured and useful.
- Summary created at `.planning/phases/01-runtime-contract-closure/01-01-SUMMARY.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-contract-closure/01-01-SUMMARY.md`
</output>
