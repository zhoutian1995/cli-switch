---
phase: 01-runtime-contract-closure
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - docs/specs/runtime-spec.md
  - docs/IMPLEMENTATION_GAP_REPORT.md
  - cmd/_shared.ts
  - test/e2e/cli-json.test.ts
autonomous: true
requirements: [RT-04]
user_setup: []
must_haves:
  truths:
    - Public error codes are documented as implemented, partial, or planned.
    - JSON envelope behavior remains stable.
  artifacts:
    - Runtime spec contains the error-code inventory.
    - Tests cover representative resolver, input, gateway, and runtime errors.
  key_links:
    - Documentation matches actual code paths after Plans 01 and 02.
---

<objective>
Document and test runtime error-code closure.

Purpose: Make the public failure surface predictable for scripts and upper-level agents.
Output: Updated runtime spec, gap report, and JSON error coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-contract-closure/01-CONTEXT.md
@docs/specs/runtime-spec.md
@docs/IMPLEMENTATION_GAP_REPORT.md
@cmd/_shared.ts
@cmd/run.ts
@cmd/resolve.ts
@test/e2e/cli-json.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory current error codes</name>
  <files>docs/specs/runtime-spec.md, docs/IMPLEMENTATION_GAP_REPORT.md</files>
  <read_first>cmd/_shared.ts, cmd/run.ts, cmd/resolve.ts, src/core/resolver/service.ts, docs/specs/runtime-spec.md</read_first>
  <action>Build an error-code inventory from actual code. Mark each code as implemented, partially implemented, or planned. Include at least INPUT_ERROR, TOOL_NOT_FOUND, MODEL_NOT_FOUND, ADAPTER_NOT_FOUND, RESOLVE_CONFLICT, PLATFORM_UNSUPPORTED, BINARY_NOT_FOUND, GATEWAY_ACP_CONFLICT, and strategy/runtime failure codes that exist after Plans 01 and 02.</action>
  <verify>rg \"code: '|code: \\\"|[A-Z_]+\" cmd src docs/specs/runtime-spec.md</verify>
  <acceptance_criteria>
    - Runtime spec distinguishes current implemented codes from v2 target codes.
    - Gap report no longer claims already-covered codes are missing.
  </acceptance_criteria>
  <done>Error-code documentation is accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize JSON error rendering where needed</name>
  <files>cmd/_shared.ts, cmd/run.ts, cmd/resolve.ts</files>
  <read_first>cmd/_shared.ts, cmd/run.ts, cmd/resolve.ts, test/e2e/cli-json.test.ts</read_first>
  <action>Use existing envelope helpers for JSON failures where any path still hand-rolls inconsistent output. Do not change the envelope fields or schema_version.</action>
  <verify>npm test -- test/e2e/cli-json.test.ts</verify>
  <acceptance_criteria>
    - JSON outputs include ok=false and error.code for covered failures.
    - warnings and diagnostics remain arrays.
  </acceptance_criteria>
  <done>JSON failure rendering is consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Add representative error golden coverage</name>
  <files>test/e2e/cli-json.test.ts</files>
  <read_first>test/e2e/cli-json.test.ts</read_first>
  <action>Add or update tests for representative error categories: input error, resolve conflict, model not found, gateway/acp conflict, and one runtime/preflight error introduced in Plan 02. Keep tests deterministic and avoid requiring real agent binaries.</action>
  <verify>npm test -- test/e2e/cli-json.test.ts</verify>
  <acceptance_criteria>
    - Tests assert exact error.code strings.
    - Tests do not depend on network or installed agent CLIs.
  </acceptance_criteria>
  <done>Error-code closure is test-covered.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test -- test/e2e/cli-json.test.ts test/unit/resolver-contract.test.ts
- [ ] npm run build
- [ ] git diff --check
</verification>

<success_criteria>

- RT-04 is satisfied.
- Runtime spec and gap report match implementation reality.
- Phase 1 can be verified as complete after all three summaries exist.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-contract-closure/01-03-SUMMARY.md`
</output>
