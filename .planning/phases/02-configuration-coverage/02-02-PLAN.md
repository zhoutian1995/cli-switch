---
phase: 02-configuration-coverage
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - cmd/run.ts
  - cmd/env.ts
  - cmd/dry-run (in cmd/run.ts)
  - src/core/gateway/index.ts
  - test/unit/config-integration.test.ts
  - test/e2e/cli-json.test.ts
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03]
user_setup: []
must_haves:
  truths:
    - cmd/run.ts loads config before gateway/tier resolution.
    - loadGatewayConfig() receives config-derived overrides for baseUrl, apiKey, models, defaultTier, agentKeys.
    - resolveTier(capability, config.routing, options.tier) receives RoutingConfig from loaded config.
    - CLI flags (--tier, --execution, --agent) still override config values.
    - Default execution strategy comes from config when CLI did not pass --execution.
    - Dry-run JSON includes config source metadata (global.loaded, project.loaded).
    - env command shows config file status in config_files array.
  artifacts:
    - run command loads and uses config for gateway, routing, tier, strategy.
    - Dry-run JSON extended with config sources.
    - env command extended to show YAML config file status.
    - Integration tests for config→gateway and config→tier wiring.
  key_links:
    - cmd/run.ts line 114-116 is the TODO: PR4 wiring point.
    - resolveTier() already accepts RoutingConfig but cmd/run.ts passes undefined.
    - loadGatewayConfig() already accepts Partial<GatewayConfig> overrides.
---

<objective>
Wire loaded config into gateway, routing, tier, and strategy resolution in the run command.

Purpose: Make config values actually affect runtime behavior — gateway overrides, tier defaults, and execution strategy defaults. CLI flags remain highest priority.
Output: Updated cmd/run.ts, updated cmd/env.ts, integration tests.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configuration-coverage/02-CONTEXT.md
@cmd/run.ts
@src/core/gateway/index.ts
@src/core/config/loader.ts
</execution_context>

<context>
@.planning/phases/02-configuration-coverage/02-CONTEXT.md
@cmd/run.ts
@src/core/gateway/index.ts
@src/core/config/loader.ts
@src/core/router/tier-resolver.ts
@cmd/env.ts
@test/e2e/cli-json.test.ts
@test/unit/tier-resolver.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire config into gateway resolution in cmd/run.ts</name>
  <files>cmd/run.ts</files>
  <read_first>cmd/run.ts, src/core/gateway/index.ts, src/core/config/loader.ts, .planning/phases/02-configuration-coverage/02-CONTEXT.md (decisions D-17, D-20)</read_first>
  <action>Update cmd/run.ts to:

1. Import loadConfig from src/core/config/index.js.
2. Call loadConfig() early in the run action (before gateway and tier resolution).
3. If config has errors (CONFIG_INVALID), add them to warnings array and continue with whatever partial config loaded (graceful degradation). Do not abort execution for config issues unless the user explicitly runs a config command.
4. Build a Partial<GatewayConfig> overrides object from config.gateway (if loaded):
   - apiKey: from config.gateway.api_key (only if present — env still takes precedence via loadGatewayConfig's own logic).
   - baseUrl: from config.gateway.base_url.
   - models: from config.gateway.models.
   - defaultTier: from config.gateway.default_tier.
   - agentKeys: from config.gateway.agent_keys.
5. Pass these overrides to loadGatewayConfig(overrides).

This preserves the existing env > overrides precedence in loadGatewayConfig while adding config as a new layer below env.

Remove the TODO comment on line 115.
</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Config gateway fields flow into loadGatewayConfig overrides.
    - Env vars still override config values (existing behavior).
    - Config errors added to warnings, not blocking.
  </acceptance_criteria>
  <done>Gateway resolution uses config-derived overrides.</done>
</task>

<task type="auto">
  <name>Task 2: Wire config into tier and strategy resolution</name>
  <files>cmd/run.ts</files>
  <read_first>cmd/run.ts, src/core/router/tier-resolver.ts, src/types/config.ts, .planning/phases/02-configuration-coverage/02-CONTEXT.md (decisions D-18, D-19)</read_first>
  <action>Update cmd/run.ts to:

1. Pass config.routing to resolveTier():
   - Change `resolveTier(capability, undefined, options.tier)` to `resolveTier(capability, config?.routing, options.tier)`.
   - This enables config-defined tier_default and capability_tier_override.

2. Wire config.execution.default_strategy into strategy resolution:
   - When options.execution is not set AND config has execution.default_strategy, use config's default as the strategyName instead of auto-selecting.
   - The existing selectStrategy(capability) remains the fallback when no config default either.
   - Validate the config-provided strategyName with isValidStrategy() before using it.

Follow decisions D-18 (routing config to resolveTier) and D-19 (config default strategy only when CLI didn't pass --execution).
</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - resolveTier receives routing config from loaded config.
    - config.execution.default_strategy used when --execution not provided.
    - CLI --tier and --execution still override config.
  </acceptance_criteria>
  <done>Tier and strategy resolution use config values.</done>
</task>

<task type="auto">
  <name>Task 3: Extend dry-run JSON and env command with config metadata</name>
  <files>cmd/run.ts, cmd/env.ts</files>
  <read_first>cmd/run.ts, cmd/env.ts, .planning/phases/02-configuration-coverage/02-CONTEXT.md (decisions D-14, D-20)</read_first>
  <action>1. In cmd/run.ts dry-run output, add a config section:
   ```json
   "config": {
     "global": { "loaded": true, "path": "/Users/.../config.yaml" },
     "project": { "loaded": false, "path": "./.cli-switch.yaml" }
   }
   ```
   Only add this as an additive field; do not restructure existing dry-run shape (D-20).

2. In cmd/env.ts, add config YAML file status to config_files array:
   - Global config.yaml at paths.configDir + '/config.yaml'.
   - Project .cli-switch.yaml at cwd.
   Show exists=true/false and loaded=true/false for each.

3. Extend collectEnvironment or the env output to include a config section showing the merged effective config (redacted) when --json is used. This gives users a single command to see what config is active.
</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Dry-run JSON includes config source metadata.
    - env command shows YAML config file status.
    - Existing JSON shapes not broken (additive fields only).
  </acceptance_criteria>
  <done>Config metadata visible in dry-run and env output.</done>
</task>

<task type="auto">
  <name>Task 4: Write integration tests</name>
  <files>test/unit/config-integration.test.ts, test/e2e/cli-json.test.ts</files>
  <read_first>cmd/run.ts, src/core/config/loader.ts, test/e2e/cli-json.test.ts, test/unit/tier-resolver.test.ts</read_first>
  <action>Write integration tests:

config-integration.test.ts (unit level, using loadConfig with tmp dirs):
- Config gateway.api_key + base_url flow into loadGatewayConfig overrides correctly.
- Config routing.capability_tier_override changes resolveTier output.
- Config routing.tier_default used for unknown capabilities.
- Config execution.default_strategy used when no CLI --execution.
- CLI --tier overrides config routing.
- CLI --execution overrides config default_strategy.
- Config errors (invalid YAML) produce warnings but don't block run.
- Secret redaction verified in effective config.

Extend test/e2e/cli-json.test.ts:
- If dry-run has e2e coverage, verify config section appears in JSON output.
- Verify config source metadata shape.
- Add golden snapshot if the test file uses snapshot patterns.

All tests must use tmp directories, never real user config paths.
</action>
  <verify>npx vitest run -- test/unit/config-integration.test.ts test/e2e/cli-json.test.ts</verify>
  <acceptance_criteria>
    - All integration tests pass.
    - Config→gateway and config→tier wiring verified.
    - No regressions in existing e2e tests.
  </acceptance_criteria>
  <done>Config integration fully tested.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx tsc --noEmit -p tsconfig.json
- [ ] npx vitest run -- test/unit/config-integration.test.ts test/e2e/cli-json.test.ts
- [ ] npx vitest run (full suite — no regressions)
- [ ] npm run build
- [ ] git diff --check
</verification>

<success_criteria>

- CFG-01: Global config values affect gateway/routing/tier/strategy.
- CFG-02: Project config deep-merges over global in run command.
- CFG-03: CLI flags override project and global config.
- Dry-run and env commands show config metadata.
- No regressions in existing tests.
- Summary created at `.planning/phases/02-configuration-coverage/02-02-SUMMARY.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-coverage/02-02-SUMMARY.md`
</output>
