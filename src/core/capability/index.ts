/**
 * Capability module — intent → capability mapping.
 *
 * @example
 * ```ts
 * import { resolveCapability } from './capability/index.js';
 * const capId = resolveCapability(intent);
 * ```
 */

export { resolveCapability } from './resolver.js';
